# Maintenance record

Fixed whole-tree balance validation to inspect all descendants. Completed the previously empty optional linear-time balance method; this is a new maintenance implementation, not original submitted work.

The original directory is unchanged. This repository contains the maintained version. The unchanged recovered files remain in the local archive.
